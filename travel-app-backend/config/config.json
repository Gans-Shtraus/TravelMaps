{
  "development": {
    "username": "postgres",
    "password": "0259",
    "database": "travel_app",
    "host": "localhost",
    "port": "5432",
    "dialect": "postgresql"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "travel_app_test",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "dialect": "${DB_DIALECT}"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "travel_app_production",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "dialect": "${DB_DIALECT}"
  }
}
